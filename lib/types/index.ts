export type { Blog } from "./blog";

export type FAQ = {
  question: string;
  answer: string;
};

export type Testimonial = {
  name: string;
  role: string;
  avatar: string;
  rating: number;
  body: string;
};
